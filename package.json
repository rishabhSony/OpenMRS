{
  "name": "openmrs-enterprise",
  "version": "1.0.0",
  "description": "Enterprise Medical Record System inspired by OpenMRS 3.x architecture",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/app-shell",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "keywords": [
    "medical",
    "healthcare",
    "emr",
    "openmrs",
    "react",
    "microfrontend"
  ],
  "author": "",
  "license": "MPL-2.0",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
